import noRelativePaths from "./no-relative-paths";
import noUrls from "./no-urls";
import requireValidPaths from "./require-valid-paths";
import useLinkForRoutes from "./use-link-for-routes";

export const rules = {
  "no-relative-paths": noRelativePaths,
  "no-urls": noUrls,
  "require-valid-paths": requireValidPaths,
  "use-link-for-routes": useLinkForRoutes,
};
